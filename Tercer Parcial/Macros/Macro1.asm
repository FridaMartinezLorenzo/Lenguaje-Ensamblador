INICIO MACRO
    MOV AX,@DATA
    MOV DS,AX
    MOV DX,AX
ENDM

PRINT MACRO MENSAJE
    PUSH DX
    PUSH AX
    LEA DX,MENSAJE
    MOV AH,09H
    INT 21H
    POP AX
    POP DX
ENDM

POSICION MACRO FILA,COLUMNA
    PUSH AX
    PUSH BX
    PUSH DX
    MOV AH,02H
    MOV DH,FILA
    MOV DL,COLUMNA
    MOV BH,0
    INT 10H
    POP DX
    POP BX
    POP AX
ENDM
    

.MODEL SMALL
.STACK 64
.DATA 
    MSJ DB 'ESTE ES MI PIMER MACRO',10,13,'$'
    
.CODE
MAIN PROC

   INICIO
   POSICION 8,6
   PRINT MSJ
   
   
   MOV AX,4C00H
   INT 21H
   
MAIN ENDP
end MAIN